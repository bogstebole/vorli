---
phase: 02-intelligent-context
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - "Receipt Tracker Tests/VorliContextBuilderTests.swift"
  - "Receipt Tracker Tests/TimeWindowTests.swift"
  - "Receipt Tracker Tests/VorliServiceTests.swift"
autonomous: false
requirements: [CTX-03, CTX-04, CTX-05]

must_haves:
  truths:
    - "xcodebuild can discover and compile the Receipt TrackerTests scheme without error"
    - "VorliContextBuilderTests.swift contains failing test stubs for ciljevi block logic"
    - "TimeWindowTests.swift contains failing test stubs for TimeWindow enum round-trip"
    - "VorliServiceTests.swift contains failing test stubs for KATEGORIZACIJA system prompt content"
  artifacts:
    - path: "Receipt Tracker Tests/VorliContextBuilderTests.swift"
      provides: "Failing test stubs for CTX-03 (ciljevi block, preostalo_meseci, finansije guard)"
      exports: [VorliContextBuilderTests]
    - path: "Receipt Tracker Tests/TimeWindowTests.swift"
      provides: "Failing test stubs for CTX-04 (TimeWindow enum round-trip, fallback)"
      exports: [TimeWindowTests]
    - path: "Receipt Tracker Tests/VorliServiceTests.swift"
      provides: "Failing test stubs for CTX-05 (KATEGORIZACIJA section in system prompt)"
      exports: [VorliServiceTests]
  key_links:
    - from: "Receipt Tracker Tests/ (XCTest bundle target)"
      to: "Receipt Tracker (app target)"
      via: "@testable import Receipt_Tracker"
      pattern: "@testable import"
---

<objective>
Create the XCTest unit test target and write RED test stubs for all Phase 2 requirements so that subsequent implementation plans have automated verify commands.

Purpose: Nyquist compliance — no plan in this phase can have a green automated verify until these stubs exist. Wave 0 makes the test infrastructure real before any production code changes.
Output: XCTest bundle "Receipt Tracker Tests" with three test files containing failing stubs covering CTX-03, CTX-04, and CTX-05.
</objective>

<execution_context>
@/Users/bogste/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bogste/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-intelligent-context/02-CONTEXT.md
@.planning/phases/02-intelligent-context/02-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Add XCTest unit test bundle target in Xcode</name>
  <files>Receipt Tracker.xcodeproj</files>
  <action>Human-only Xcode GUI action: add a Unit Testing Bundle target named "Receipt Tracker Tests" targeting "Receipt Tracker". No CLI equivalent exists for adding a new test bundle target to an xcodeproj.</action>
  <verify>
    <automated>MISSING — human must complete Xcode target setup before automated verify is possible</automated>
  </verify>
  <done>Scheme "Receipt TrackerTests" exists and xcodebuild test compiles it without build errors.</done>
  <what-built>Nothing yet — this is a human-required Xcode GUI action that has no CLI equivalent for adding a test bundle target to an existing Xcode project.</what-built>
  <how-to-verify>
    1. Open Receipt Tracker.xcodeproj in Xcode
    2. File > New > Target
    3. Select "Unit Testing Bundle" under iOS
    4. Name: "Receipt Tracker Tests"
    5. Target to test: "Receipt Tracker"
    6. Language: Swift
    7. Click Finish
    8. When prompted to activate the scheme, click Activate
    9. Verify: the "Receipt TrackerTests" scheme appears in the scheme picker
    10. Delete the auto-generated `Receipt_TrackerTests.swift` placeholder file (we will write our own)
    11. Run: xcodebuild test -scheme "Receipt TrackerTests" -destination "platform=iOS Simulator,name=iPhone 16" — should compile with no test failures (no tests yet)
  </how-to-verify>
  <resume-signal>Type "done" when the scheme builds successfully</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test stubs for CTX-03, CTX-04, CTX-05</name>
  <files>
    Receipt Tracker Tests/VorliContextBuilderTests.swift,
    Receipt Tracker Tests/TimeWindowTests.swift,
    Receipt Tracker Tests/VorliServiceTests.swift
  </files>
  <behavior>
    VorliContextBuilderTests (CTX-03):
    - testCiljeviBlock: build() with one SavingsGoal → output JSON contains "ciljevi" key inside "finansije" — FAILS (SavingsGoal doesn't exist yet)
    - testExpiredGoalClampedToZero: goal.rok is yesterday → preostalo_meseci in JSON == 0 — FAILS
    - testFinansijeCreatedForGoalsOnly: build() with no budget, no income, but one goal → JSON contains "finansije" key — FAILS

    TimeWindowTests (CTX-04):
    - testAllCasesRoundTrip: for each raw value in ["this_month","last_month","this_week","last_week","recent"] → TimeWindow(rawValue:) != nil — FAILS (TimeWindow doesn't exist yet)
    - testFallbackOnUnknownRawValue: TimeWindow(rawValue: "garbage") == nil — FAILS

    VorliServiceTests (CTX-05):
    - testSystemPromptContainsKategorizacija: buildSystemPrompt output contains "KATEGORIZACIJA" — FAILS (section not added yet)
    - testSystemPromptMerchantNames: buildSystemPrompt output contains "Maxi" and "Lidl" — FAILS
  </behavior>
  <action>
    Create three test files with @testable import Receipt_Tracker and XCTestCase subclasses.

    All three files must compile cleanly even though tests fail. Use XCTFail("not implemented: {feature}") as the stub body for tests that reference types that don't exist yet — do NOT reference non-existent types directly (that is a compile error, not a test failure).

    Pattern for stubs referencing missing types:
    ```swift
    func testCiljeviBlock() {
        XCTFail("CTX-03: SavingsGoal model not yet implemented — implement in Plan 02")
    }
    ```

    VorliContextBuilderTests.swift:
    ```swift
    import XCTest
    @testable import Receipt_Tracker

    final class VorliContextBuilderTests: XCTestCase {
        func testCiljeviBlock() {
            XCTFail("CTX-03: SavingsGoal model not yet implemented — implement in Plan 02")
        }
        func testExpiredGoalClampedToZero() {
            XCTFail("CTX-03: SavingsGoal model not yet implemented — implement in Plan 02")
        }
        func testFinansijeCreatedForGoalsOnly() {
            XCTFail("CTX-03: finansije guard for goals-only not yet implemented — implement in Plan 02")
        }
    }
    ```

    TimeWindowTests.swift:
    ```swift
    import XCTest
    @testable import Receipt_Tracker

    final class TimeWindowTests: XCTestCase {
        func testAllCasesRoundTrip() {
            XCTFail("CTX-04: TimeWindow enum not yet implemented — implement in Plan 04")
        }
        func testFallbackOnUnknownRawValue() {
            XCTFail("CTX-04: TimeWindow enum not yet implemented — implement in Plan 04")
        }
    }
    ```

    VorliServiceTests.swift:
    ```swift
    import XCTest
    @testable import Receipt_Tracker

    final class VorliServiceTests: XCTestCase {
        func testSystemPromptContainsKategorizacija() {
            XCTFail("CTX-05: KATEGORIZACIJA section not yet added to buildSystemPrompt() — implement in Plan 03")
        }
        func testSystemPromptMerchantNames() {
            XCTFail("CTX-05: KATEGORIZACIJA section not yet added to buildSystemPrompt() — implement in Plan 03")
        }
    }
    ```

    Add @testable import Receipt_Tracker at the top of each file.
  </action>
  <verify>
    <automated>xcodebuild test -scheme "Receipt TrackerTests" -destination "platform=iOS Simulator,name=iPhone 16" 2>&1 | tail -20</automated>
  </verify>
  <done>All three test files compile. xcodebuild test run completes with test failures (not compile errors). The scheme "Receipt TrackerTests" is usable by Plans 02-04 for their automated verify commands.</done>
</task>

</tasks>

<verification>
xcodebuild test -scheme "Receipt TrackerTests" -destination "platform=iOS Simulator,name=iPhone 16" 2>&1 | grep -E "(FAILED|error:|warning:|Test Suite)" | head -30
</verification>

<success_criteria>
- XCTest bundle target "Receipt Tracker Tests" exists and compiles
- Three test files exist with XCTestCase subclasses
- xcodebuild test runs and reports test failures (not compile errors)
- No compile errors in any test file
- Scheme "Receipt TrackerTests" is ready for Plans 02-04 to verify against
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligent-context/02-01-SUMMARY.md`
</output>
